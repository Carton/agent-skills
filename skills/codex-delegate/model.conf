# codex-delegate model pin — the ONLY place to change the model or reasoning effort.
# Sourced by every command template in SKILL.md.
MODEL="gpt-6-astra"   # e.g. later: MODEL="gpt-7-whatever"
EFFORT="low"          # codex reasoning effort: low | medium | high

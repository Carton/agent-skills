当前这个 skill 在最后一步还需要改进：
1. 当前的这个 verify_cleanup.sh 脚本还不能完全保证AI 不偷懒：必须要检查大小减少了至少XX%，然后不能含有类似 "fcn.", "pcVar" 等这样的字符串，才算是真正的完成清理工作。
2. 上下文管理，在做最后一不AI 来逐个清理函数的时候，如何有效管理上下文，是一个很大的问题。首先一般需要一个类似于函数和关键数据结构的概览，方便整体解析。但是又不能把所有东西都塞进上下文里，所以需要一个 smart 的策略来决定把什么塞进去。这块需要研究一下别的skill 是怎么做的
3. 怎么保证逐个处理每个文件的时候不偷懒。